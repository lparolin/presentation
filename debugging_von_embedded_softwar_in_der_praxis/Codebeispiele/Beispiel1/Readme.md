# Printf Timing Demo

Dieses Projekt demonstriert den Einfluss von `printf` auf die Zeitmessung,

## 🚀 Build Instructions

1. Erstelle ein Build-Verzeichnis:

   ```bash
   mkdir build
   cd build
   ```

2. Führe CMake aus:
   `cmake .. `

3. Baue das Projekt:
   `cmake --build .`

4. Starte das Program:
   `./printf_timing_demo`
